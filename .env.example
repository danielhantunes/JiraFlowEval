# Copy to .env and set your values. Do not commit .env.
# In GitHub Actions these are set via Repository Secrets and Variables (see README).

# Required for LLM evaluation:
OPENAI_API_KEY=sk-your-openai-api-key

# Main – Azure credentials (passed into pipeline container; use Secrets/Variables in CI):
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
AZURE_ACCOUNT_URL=
AZURE_BLOB_NAME=
AZURE_CONTAINER_NAME=

# Optional – local input file for candidate repos that read from file:
RAW_INPUT_FILENAME=jira_issues_raw.json
