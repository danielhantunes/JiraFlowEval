# Copy to .env and set your values. Do not commit .env.
# Required for LLM evaluation:
OPENAI_API_KEY=sk-your-openai-api-key

# Optional: use LLM to get run command from each repo's README (requires OPENAI_API_KEY).
# When set, the command is always run in Docker for safe execution.
# USE_README_RUN_COMMAND=1

# Optional: run each repo's pipeline inside a Docker container (python:3.12-slim)
# RUN_IN_DOCKER=1

# If candidate repos use Azure Service Principal (e.g. third-party account, read-only),
# set these so they are passed into the container when running in Docker:
# AZURE_CLIENT_ID=
# AZURE_TENANT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_SUBSCRIPTION_ID=

# Optional overrides (defaults work with Docker):
# TEMP_REPOS_DIR=/app/temp_repos
# OUTPUT_DIR=/app/output
# REPO_EVALUATOR_ROOT=/app
# SCORING_CONFIG_PATH=/app/config/scoring.yaml
